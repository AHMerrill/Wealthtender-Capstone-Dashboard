# ── API & Dashboard (runtime) ────────────────────────────────────────
# These are the only packages needed to serve the API and dashboard.
# Both Dockerfiles install from this file.
fastapi==0.119.0
uvicorn==0.29.0
pydantic==2.11.0
pandas==2.2.2
plotly==5.20.0
dash==2.16.1
requests==2.31.0
gunicorn>=21.2.0
